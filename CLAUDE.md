# SalatOnTime — الصلاة على وقتها

Flutter app (Android + iOS) whose core value: tell the user the **exact moment to
leave home** so they arrive at their mosque for adhan or iqama. Open source
(GPL-3.0), headed for real Play Store / App Store release.
Repo: https://github.com/MamoonAlmazloom/SalatOnTime

## Critical commands (Windows machine quirks)

- Flutter SDK: `C:\src\flutter` (3.44.4 stable). Android SDK present; no Mac, so iOS builds deferred.
- **Always pass the target explicitly** — plain `flutter build/run` fails on this
  machine with `/main.dart not found` (path-separator bug):
  `flutter run --target=lib/main.dart` / `flutter build appbundle --release --target=lib/main.dart`
- Test/analyze: `flutter test`, `flutter analyze` (keep both green; ~29 tests).
- l10n: edit `lib/l10n/app_{en,ar}.arb`, then `flutter gen-l10n` (outputs real files in `lib/l10n/`).
- Emulator: `Pixel_3a_API_34_extension_level_7_x86_64`; if it freezes, cold-boot with
  `-no-snapshot-load -gpu swiftshader_indirect`. Set GPS: `adb emu geo fix 46.6753 24.7136` (Riyadh).
- PowerShell 5.1: never put `"` inside `git commit -m` here-strings (arg mangling).

## Architecture

`lib/domain` (models + pure use-cases, unit-tested) / `lib/data` (services, repositories)
/ `lib/ui` (features/{home,onboarding,settings}, core widgets). MVVM with ChangeNotifier;
no Riverpod. Engine: `adhan_dart` Umm al-Qura offline; leave_time = arrival target
(adhan or adhan+iqama offset) − travel − wudu − per-prayer bathroom − margin.
Notifications: `flutter_local_notifications` exact zoned schedules, 3 days ahead,
10 rotating ayah/hadith messages (`lib/ui/core/alert_messages.dart`), standard or
alarm-style channel. Theme/locale: `themeModeNotifier` / `localeNotifier`
(`lib/ui/core/theme_controller.dart`), persisted in `SettingsRepository`.

## Design language ("premium/flagship" — mirror docs/privacy-policy.html, which the user loves)

- Emerald `#0E7C66` → deep `#07473C` gradients (AppTheme.heroGradient), gold accent `#E8B84B` (AppTheme.gold)
- Tajawal font (bundled, weights 400/500/700); Arabic-first, full RTL
- Rounded 22–28px cards, soft glow shadows, glass panels (white ~10% alpha on gradients)
- Section titles: gold inline-start bar (see privacy page h2 style)
- Launcher icon: white mosque + gold clock badge on emerald (assets/icon/, generated by scratchpad PIL script)

## Store identity (permanent — do not change)

- applicationId / bundle ID: `io.github.mamoonalmazloom.salatontime`
- Display name: "Salat On Time" (en) / "الصلاة على وقتها" (ar)
- Signing: `android/app/upload-keystore.jks` + `android/key.properties` (gitignored, this machine only — user must back up)
- Privacy policy: https://mamoonalmazloom.github.io/SalatOnTime/privacy-policy (docs/ on Pages)
- Positioning: privacy-first, "verify don't trust", never name competitor apps in official docs

## Next planned work (user request, not yet started)

**Flagship UI pass on the app itself:**
1. Fix in-app naming: `appTitle` in ARB files still says "SalatApp"/"تطبيق الصلاة" →
   must become "Salat On Time" / "الصلاة على وقتها" (also welcomeBody mentions SalatApp).
2. Incorporate the app logo (assets/icon/icon_foreground.png) into the UI —
   e.g. emblem in a Home gradient hero header (like the privacy page header).
3. Redesign the Home header to feel professional/flagship (research current patterns);
   consider merging date/mosque/countdown into one gradient hero with the glass panel,
   gold-bar section headers below (shared widget, reuse in Settings).
4. General flagship polish app-wide, borrowing from the privacy page design.
Skill available for graphics work: `.agents/skills/canvas-design` (official Anthropic).

Then: store listing assets (feature graphic via canvas-design, screenshots, ar/en copy),
real-device notification testing, background rescheduling (WorkManager) so alerts
don't run out after ~3 days unopened, Jumu'ah handling.
